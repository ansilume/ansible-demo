[defaults]
# Tells ansible-lint this is an Ansible project root so it discovers
# all playbooks and roles correctly.
inventory = inventory
roles_path = roles
collections_path = collections
host_key_checking = false
retry_files_enabled = false
stdout_callback = yaml
interpreter_python = auto_silent
